/**
 * a presigned url for get or put object
 * @param {string} location - The location of the file to be uploaded.
 * @param {string} method - The method of the request (GET or PUT).
 *
 * */
export const signedURL = async ({ location, method }) => {
  try {
  } catch (e) {
    return null;
  }
};
